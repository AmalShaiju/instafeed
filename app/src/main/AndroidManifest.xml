<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nc.prog1415">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Instafeed">
        <activity
            android:name=".activities.others.SplashScreenActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.donor_view.activities.DonorViewActivity"
            android:exported="true" />
        <activity
            android:name=".activities.claimer_view.activities.ClaimerViewActivity"
            android:exported="true" />
        <activity
            android:name=".activities.donor_view.activities.PrCreateActivity"
            android:exported="true"
            android:parentActivityName=".activities.donor_view.activities.DonorViewActivity" />
        <activity
            android:name=".activities.user_register.RegisterActivity"
            android:exported="true"
            android:parentActivityName=".activities.login.LoginActivity" />
        <activity
            android:name=".activities.login.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>